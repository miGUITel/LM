### 🔢 **Tipos de datos primitivos en JavaScript**

JavaScript tiene varios **tipos básicos** que no son objetos y se almacenan **por valor**.

---

### 🟣 **1. String** → cadenas de texto

Se escriben entre comillas simples `' '`, dobles `" "` o con *backticks* `` ` ` `` (plantillas literales).

```js
let nombre = "Ana";
let saludo = `Hola, ${nombre}!`; // plantilla literal
```

---

### 🔵 **2. Number** → números

JavaScript **usa el formato IEEE-754 (doble precisión, 64 bits)** para todos los números, sean enteros o decimales.
Esto puede producir **pequeños errores de redondeo**:

```js
0.1 + 0.2 === 0.3 // ❌ false (0.30000000000000004)
```

Incluye también valores especiales:

* **`Infinity`** y **`-Infinity`** → resultado de divisiones por cero.

  ```js
  5 / 0  // Infinity
  ```
* **`NaN` (Not a Number)** → resultado de una operación numérica inválida.

  ```js
  "hola" * 3  // NaN
  ```

---

### 🟢 **3. Boolean** → valores lógicos

Solo puede ser **`true`** o **`false`**.

```js
let activo = true;
let mayor = 18 > 21; // false
```

---

### 🟡 **4. Undefined**

Indica que **una variable existe pero no tiene valor asignado**.

```js
let x;
console.log(x); // undefined
```

---

### ⚫ **5. Null**

Representa la **ausencia intencionada de valor**.

```js
let usuario = null; // se sabe que no hay valor
```

---

📌 **Diferencia clave:**

* `undefined` → el valor **no se ha asignado**.
* `null` → el valor se ha asignado **intencionadamente como vacío**.

---

🧠 En resumen:

| Tipo      | Ejemplo                         | Significado             |
| --------- | ------------------------------- | ----------------------- |
| String    | `"Hola"`                        | Texto                   |
| Number    | `3.14`, `-2`, `Infinity`, `NaN` | Números                 |
| Boolean   | `true`, `false`                 | Lógico                  |
| Undefined | `let x;`                        | Sin valor asignado      |
| Null      | `let x = null;`                 | Valor vacío intencional |

---
