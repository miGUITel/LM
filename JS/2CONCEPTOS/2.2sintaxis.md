### 🧩 Sintaxis básica de JavaScript

* **Punto y coma (`;`)**
  Indica el **final de una instrucción**, aunque en muchos casos el intérprete lo añade automáticamente.
  👉 Se recomienda usarlo siempre por **claridad y evitar errores**.

  ```js
  let x = 5;
  let y = 10;
  console.log(x + y);
  ```

* **Mayúsculas y minúsculas importan (case sensitive)**
  JavaScript **distingue entre mayúsculas y minúsculas**.

  ```js
  let Usuario = "Ana";
  let usuario = "Luis";
  console.log(Usuario); // Ana
  console.log(usuario); // Luis
  ```

  👉 Son **dos variables distintas**.

* **Legibilidad del código**
  Usa **tabulaciones, saltos de línea y espacios** para que el código sea más fácil de leer:

  ```js
  if (edad >= 18) {
      console.log("Eres mayor de edad");
  } else {
      console.log("Eres menor de edad");
  }
  ```

* **Palabras reservadas**
  No pueden usarse como nombres de variables:
  `if`, `else`, `for`, `while`, `function`, `return`, `const`, `let`, `class`, `new`, `break`, `true`, `false`, etc.

* **Reglas para nombres de variables**

  * Solo pueden contener **letras**, **números**, **guiones bajos** (`_`) o **símbolos de dólar** (`$`).
  * **No pueden empezar con número.**
  * **No se permiten espacios ni guiones medios (-).**
  * Se recomienda usar **camelCase** para variables (`miVariable`) y **MAYÚSCULAS_CON_GUIONES_BAJOS** para constantes (`PI_VALOR`).

  ✅ Ejemplos válidos:

  ```js
  let edad;
  let nombre_usuario;
  const PI_VALOR = 3.1416;
  let $contador = 0;
  ```

  ❌ Ejemplos no válidos:

  ```js
  let 2numero;      // no puede empezar por número
  let mi-variable;  // el guion medio no está permitido
  let let;          // palabra reservada
  ```
