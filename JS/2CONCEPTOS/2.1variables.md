[JS](../indice.md)

En **JavaScript**, una **variable** sirve para **guardar un valor** (número, texto, booleano, etc.) que podremos **usar o cambiar** más adelante.

### 🔹 Declaración de variables

Se declaran con las palabras clave **`let`** o **`const`**:

* **`let`** → crea una variable **que puede cambiar** su valor.

  ```js
  let edad = 20;
  edad = 21; // ✅ permitido
  ```

* **`const`** → crea una variable **que no puede reasignarse**.

  ```js
  const nombre = "Ana";
  nombre = "Luis"; // ❌ error
  ```

### 🔹 Funcionamiento general

* Las variables tienen **ámbito de bloque** (solo existen dentro de las llaves `{}` donde se declaran).
* No hace falta indicar el tipo de dato: JavaScript lo **detecta automáticamente** (tipado dinámico).
* Es buena práctica **usar `const` siempre que no necesites cambiar el valor**, y `let` solo cuando realmente lo necesites.

👉 Ejemplo completo:

```js
const mensaje = "Hola";
let contador = 1;

console.log(mensaje + " número " + contador); // Hola número 1

contador++;
console.log(contador); // 2
```

> No utilizaremos `var`porque es fuente de errores.