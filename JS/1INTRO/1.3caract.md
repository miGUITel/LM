[JS](../indice.md)

## Características básicas de JS

* **Interpretado:**
  El código se ejecuta directamente en el navegador (sin necesidad de compilarlo antes).
  👉 Permite probar y modificar el código rápidamente, pero puede ser algo más lento que un lenguaje compilado.

* **Tipado dinámico:**
  Las variables **no tienen un tipo fijo**.
  👉 Una misma variable puede guardar un número, luego un texto, etc.

* **Débilmente tipado:**
  JavaScript **convierte automáticamente los tipos** cuando hace operaciones.
  👉 Esto puede facilitar algunas tareas, pero también causar errores curiosos, como:

  ```js
  "5" + 2   // da "52" (concatena)
  "5" - 2   // da 3 (convierte a número)
  ```

Aunque suelen confundirse, **no son lo mismo**:

* 🌀 **Tipado dinámico** → se refiere **a cuándo** se conoce el tipo.

  * En JavaScript, el tipo de una variable **se determina en tiempo de ejecución**, no al declarar la variable.
  * Ejemplo:

    ```js
    let x = 5;     // x es un número
    x = "hola";    // ahora es una cadena
    ```
  * 👉 El tipo puede **cambiar durante la ejecución**.

* ⚖️ **Débilmente tipado** → se refiere **a cómo** trata los tipos distintos.

  * JavaScript permite **convertir tipos automáticamente** para intentar que una operación tenga sentido.
  * Ejemplo:

    ```js
    2 + "3"   // resultado: "23"
    ```
  * 👉 Mezcla tipos distintos (número y texto) sin error, aplicando **conversión implícita**.

---

📌 En resumen:

* **Dinámico** = el tipo **puede cambiar**.
* **Débil** = los tipos **se mezclan automáticamente**.
